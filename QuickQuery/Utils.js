/**
 * @function Preventive and traditional tactile operation
 * @param 
 * @example doSearch, mousemove, windows.scroll, windows.size
 * @description 防止频繁触发，以上场景适用 —— Vue demo
 */
export const handleDebounce = () => {
// created() { 
//   this.doSearch = _.bind(_.debounce(this.doSearch, 500), this);
// }
};
